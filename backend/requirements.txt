fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
httpx==0.27.2
python-dotenv==1.0.1
motor==3.7.1
pymongo==4.18.0
python-multipart==0.0.12

# MCP tool server + client (specs.md Phase 8). Shared by both processes: the
# backend uses the client half, mcp_server.py the server half.
#
# Held at the 1.12.x line on purpose -- mcp >= 1.13 requires pydantic >= 2.11 and
# a starlette that fastapi 0.115 rejects, so moving up means bumping fastapi and
# pydantic in the same change. The two transitive pins below exist for the same
# reason and are not incidental:
#   starlette     -- unpinned, pip resolves 1.x for mcp and breaks fastapi 0.115
#                    (which requires <0.39).
#   sse-starlette -- 3.x requires starlette >= 0.49.1; 2.1.3 is the last line
#                    compatible with the starlette above.
mcp==1.12.4
starlette==0.38.6
sse-starlette==2.1.3
# Pulled in by mcp anyway, pinned explicitly because mcp_server.py imports it
# directly to validate tool arguments against their declared schemas.
jsonschema==4.26.0

# Metrics and structured logs (see observability.py). Dependency-free, so it
# does not disturb the mcp/starlette/fastapi pin lock above.
prometheus-client==0.21.0
